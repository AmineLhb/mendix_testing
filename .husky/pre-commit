#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit: validate generated tests and lint-staged"
npm run validate:generated || { echo "validate:generated failed"; exit 1; }
npx lint-staged || { echo "lint-staged failed"; exit 1; }
