# Copy this file to .env and fill in secrets for local runs (never commit .env).
# Tool-level config only — not tied to any one app under test. Per-app
# BASE_URL and credentials live in projects/<name>/.env.example instead.
GROQ_API_KEY=
